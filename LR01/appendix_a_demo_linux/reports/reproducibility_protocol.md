# Протокол проверки воспроизводимости ЛР №1

## 1. Предмет проверки

- Дисциплина: «Искусственный интеллект в креативных технологиях».
- ЛР №1: «Запуск открытой модели text-to-image и фиксация воспроизводимого результата».
- Дата фактической проверки: 17.09.2026.
- Модель: `stabilityai/sd-turbo`.
- Закреплённая ревизия: `b261bac6fd2cf515557d5d0707481eafa0485ec2`.
- Режим: локальный инференс на CPU, без внешнего генеративного сервиса.

## 2. Параметры

- Prompt: `editorial poster concept for a local science festival, abstract geometric light installation, deep blue and amber palette, clean composition, no text, no logo, no people`
- Seed: `20260917`
- Число шагов: `1`
- Guidance scale: `0.0`
- Размер: `512 × 512`
- Тип данных: `torch.float32`
- Детерминированные алгоритмы PyTorch: `True`
- Число потоков PyTorch: `1`

## 3. Фактическая среда

- ОС: `Linux-6.18.44-x86_64-with-glibc2.39`
- Python: `3.12.14`
- PyTorch: `2.7.1+cpu`
- Torchvision: `0.22.1+cpu`
- Diffusers: `0.40.0`
- Transformers: `5.17.0`
- Accelerate: `1.15.0`
- Safetensors: `0.8.0`
- Hugging Face Hub: `1.31.0`

Полный список пакетов сохранён в `reports/environment.txt`.

## 4. Результаты двух запусков

| Проверка | Запуск 1 | Запуск 2 |
| --- | ---: | ---: |
| Время генерации, с | 93.725 | 87.451 |
| Размер файла, байт | 579958 | 579958 |
| Размер изображения | 512 × 512 | 512 × 512 |
| Цветовой режим | RGB | RGB |
| SHA-256 | `c1e668a658398f122335ec6ec7e019f52429a9b4b311dd7c678de2e507923723` | `c1e668a658398f122335ec6ec7e019f52429a9b4b311dd7c678de2e507923723` |

Побитовое сравнение: **PASS**.

Два PNG полностью совпадают; одинаковый SHA-256 подтверждает повторяемость результата
в пределах данной зафиксированной CPU-среды.

## 5. Выявленное расхождение методических указаний

Первоначальная зависимость `diffusers==0.40.0` вместе с `transformers>=4.51,<5`
не устанавливается: Diffusers 0.40.0 требует `huggingface-hub>=1.23`, тогда как
Transformers 4.x требует `huggingface-hub<1`. Для фактического запуска использован
совместимый Transformers `5.17.0`. Методические указания
следует актуализировать.

## 6. Безопасность и лицензирование

Использован нейтральный prompt без персональных данных, реальных лиц, товарных
знаков и закрытых материалов. Карточка SD-Turbo описывает модель как исследовательский
артефакт, отдельно отсылает к условиям коммерческого использования и Acceptable Use
Policy Stability AI. Перед публичным или коммерческим применением условия необходимо
проверить повторно: https://huggingface.co/stabilityai/sd-turbo

Загруженная конфигурация pipeline не содержит safety checker. Это допустимо только
для контролируемого учебного эксперимента с безопасным prompt; для открытого сервиса
нужен отдельный фильтр и политика модерации.

## 7. Границы вывода

Результат подтверждает точное совпадение двух запусков в одной среде. Он не доказывает
побитовую воспроизводимость между CPU и GPU, различными ОС, версиями библиотек или
архитектурами ускорителей. Время первого запуска включает только генерацию; загрузка
pipeline измерена отдельно: 164.382 с.

## 8. Итог

Статус проверки: **PASS**.

Артефакты позволяют проверить параметры, версии, два результата, журналы запусков и
контрольные суммы. Кэш весов и виртуальное окружение намеренно не включены в архив.
